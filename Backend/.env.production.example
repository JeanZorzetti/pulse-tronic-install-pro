# Pulse Tronic Backend - Production Environment Variables
# Copie este arquivo e ajuste os valores conforme necessário

# Server Configuration
NODE_ENV=production
PORT=3000
API_URL=https://api.pulsetronic.com.br

# Database (PostgreSQL no Easypanel)
# Use a Internal Connection URL para melhor performance
DATABASE_URL=postgresql://pulse:PAzo18**@dados_pulse:5432/pulse?sslmode=disable

# Redis (opcional - configurar depois)
# REDIS_URL=redis://redis:6379

# JWT Authentication
# IMPORTANTE: Gere novos secrets seguros com: openssl rand -base64 64
JWT_SECRET=MUDE-ESTE-SECRET-PARA-UM-VALOR-SEGURO-E-UNICO
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=MUDE-ESTE-REFRESH-SECRET-PARA-UM-VALOR-SEGURO-E-UNICO
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (Gmail SMTP)
# IMPORTANTE: Use App Password, não sua senha normal
# Gere em: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=contato@pulsetronic.com.br
SMTP_PASS=sua-senha-de-app-do-gmail-aqui
SMTP_FROM=Pulse Tronic <contato@pulsetronic.com.br>

# Admin Email (recebe notificações)
ADMIN_EMAIL=admin@pulsetronic.com.br

# WhatsApp API (opcional - Twilio)
# Configure quando estiver pronto para implementar
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_WHATSAPP_NUMBER=+14155238886
# ADMIN_WHATSAPP=+5511999999999

# Google Maps API (opcional)
# Configure quando estiver pronto para implementar
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Frontend URLs (CORS)
# Ajuste com seus domínios reais
FRONTEND_URL=https://pulsetronic.roilabs.com.br
ADMIN_URL=https://pulseadmin.roilabs.com.br

# AWS S3 para uploads (opcional - configurar depois)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_BUCKET_NAME=pulsetronic-uploads
# AWS_REGION=us-east-1

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
BCRYPT_ROUNDS=10

# Error Tracking (Sentry - opcional)
# Configure para monitoramento de erros em produção
# SENTRY_DSN=your-sentry-dsn

# ==========================================
# CHECKLIST DE CONFIGURAÇÃO:
# ==========================================
# [ ] JWT_SECRET alterado para valor único e seguro
# [ ] JWT_REFRESH_SECRET alterado para valor único e seguro
# [ ] SMTP_PASS configurado com App Password do Gmail
# [ ] FRONTEND_URL e ADMIN_URL ajustados com domínios reais
# [ ] ADMIN_EMAIL verificado
# [ ] Testar conexão com database após configurar
